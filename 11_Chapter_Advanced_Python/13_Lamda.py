# Functions created by the lambda keyword 
"""
Lambda functions are anonymous functions, meaning they do not have a name. They are often used for short, simple functions that are not going to be reused elsewhere in the code. The syntax for a lambda function is:
lambda arguments : expression
"""

square = lambda x : x*x

print(square(2))