"""
lambda functions are anonymous functions that can be defined in a single line. They are often used for short, simple functions that are not intended to be reused elsewhere in the code. The syntax for a lambda function is as follows:
lambda arguments: expression
"""

add = lambda x, y: x + y
print(add(5, 3))  # Output: 8